{
  "name": "wptechnix-wordpress-coding-standards",
  "version": "0.0.0",
  "description": "Development dependencies and tooling for WordPress coding standards",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
